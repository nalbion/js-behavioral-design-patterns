Now we can put our application together using the handlers that we have created.

Compose a chain of responsibility using the handlers that we have created.

Invoke the root handler and return the response.
